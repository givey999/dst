export function installGlobalProgressListeners() {
  // Stub — real implementation in Task 14. For now, no-op.
}

export async function uploads(root) {
  root.innerHTML = `<div class="loading">uploads (stub — implemented in Task 14)</div>`;
}
