{
  "name": "@dst/renderer",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "build": "echo 'no build — renderer is static'",
    "typecheck": "echo 'no typecheck — renderer is vanilla JS'",
    "test": "echo 'no unit tests for renderer'",
    "lint": "echo 'no lint'"
  }
}
