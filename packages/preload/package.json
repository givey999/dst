{
  "name": "@dst/preload",
  "version": "0.2.1",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint'"
  },
  "dependencies": {
    "@dst/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "electron": "^32.0.0",
    "typescript": "^5.6.0"
  }
}
